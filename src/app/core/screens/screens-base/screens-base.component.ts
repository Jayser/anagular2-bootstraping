import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'cr-base',
  templateUrl: 'screens-base.template.html',
  changeDetection: ChangeDetectionStrategy.OnPush,
})

export class ScreensBaseComponent { }
